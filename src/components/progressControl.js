export default function progressControl() {
  return (
    <>
      <section className="button-group col col-12" data-phase="address">
        <button style={{ cursor: "pointer" }} className="next">
          下一步
        </button>
      </section>
    </>
  );
}
